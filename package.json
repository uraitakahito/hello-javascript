{
  "name": "hello-javascript",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "coverage": "npx vitest run --coverage",
    "lint": "eslint .",
    "test:jest:run": "NODE_OPTIONS=\"$NODE_OPTIONS --experimental-vm-modules\" npx jest test-jest",
    "test:vitest:run": "npx vitest run",
    "rmdir": "node -e \"var fs = require('fs'); try{process.argv.slice(1).map((fpath) => fs.rmdirSync(fpath, { recursive: true }))}catch(err){console.log(err)}; process.exit(0);\""
  },
  "devDependencies": {
    "@vitest/coverage-v8": "*",
    "app-root-path": "^3.1.0",
    "child_process": "*",
    "eslint": "*",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-jest": "^28.8.3",
    "eslint-plugin-jsdoc": "*",
    "eslint-plugin-vitest": "*",
    "jest": "^29.7.0",
    "path": "^0.12.7",
    "redis": "^4.7.0",
    "supertest": "*",
    "vitest": "*",
    "winston": "^3.14.2"
  }
}
